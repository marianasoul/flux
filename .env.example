# ===== CONFIGURAÇÕES DO MEDSTUDIOS =====
# Copie este arquivo para .env e preencha com os valores apropriados

# Configurações do Servidor
NODE_ENV=development
PORT=5000

# Base URL da aplicação
NEXT_PUBLIC_BASE_URL=http://localhost:5000

# Banco de Dados
# Para desenvolvimento local (MongoDB)
DB_URI=mongodb://localhost:27017/medstudios

# Para produção (PostgreSQL)
# DATABASE_URL=postgresql://usuario:senha@host:porta/database

# Configurações do Vercel (apenas para produção)
# VERCEL_ENV=production

# Chaves de Segurança (GERE CHAVES ÚNICAS E SEGURAS)
# JWT_SECRET=sua_chave_jwt_super_secreta_aqui
# API_SECRET=sua_chave_api_super_secreta_aqui

# ===== INSTRUÇÕES =====
# 1. Renomeie este arquivo para .env
# 2. Preencha todas as variáveis com valores reais
# 3. Para JWT_SECRET e API_SECRET, use geradores de chaves seguras
# 4. NUNCA commite o arquivo .env no Git
# 5. Para produção, crie um .env.production separado